[project]
name = "dimeclass"
version = "0.1.0"
description = "Epub to TEI Transformation and Classification"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "beautifulsoup4>=4.13.4",
    "pandas>=2.3.1",
]
